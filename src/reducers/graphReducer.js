const initialState = {
    graphData: {
      labels: [],
      datasets:[]
    }
}

const graphReducer = (state = initialState, action) => {
  switch (action.type) {
    case value:

      break;

    default:
      return state;
  }
}